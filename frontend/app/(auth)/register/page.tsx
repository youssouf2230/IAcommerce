import  { RegisterForm } from '@/components/auth/register-form'; // ici on importe DataRegister par défaut
import React from 'react';

const Page = async () => {
   
    return <RegisterForm />;
}

export default Page;
